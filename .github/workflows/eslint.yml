name: Fix JS and Vue code style issues

on:
  workflow_call:
    inputs:
      node-version:
        required: true
        type: string

      package-manager:
        default: "yarn"
        type: string

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  eslint:
    if: "!contains(github.event.head_commit.message, '[ci skip]')"

    runs-on: self-hosted

    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: ${{ inputs.node-version }}
          registry-url: "https://npm.fontawesome.com/"

      - name: Authenticate private node registries
        run: |
          if [[ -n "${{ secrets.FONTAWESOME_NPM_AUTH_TOKEN }}" && "${{ secrets.FONTAWESOME_NPM_AUTH_TOKEN }}" != "" && "${{ secrets.FONTAWESOME_NPM_AUTH_TOKEN }}" != " " ]]; then
              npm config set '//npm.fontawesome.com/:_authToken' "${{ secrets.FONTAWESOME_NPM_AUTH_TOKEN }}"
          fi

      - name: Get yarn cache directory path
        if: ${{ inputs.package-manager == 'yarn' }}
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"

      - uses: actions/cache@v3
        id: yarn-cache # use this to check for `cache-hit` (`steps.yarn-cache.outputs.cache-hit != 'true'`)
        if: ${{ inputs.package-manager == 'yarn' }}
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-${{ inputs.node-version }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ inputs.node-version }}-yarn

      - name: Install node dependencies using yarn
        if: ${{ inputs.package-manager == 'yarn' }}
        run: |
          yarn install --pure-lockfile

      - name: Get pnpm cache directory path
        if: ${{ inputs.package-manager == 'pnpm' }}
        id: pnpm-cache-dir-path
        run: echo "::set-output name=dir::$(echo ~/.pnpm-store)"

      - uses: actions/cache@v3
        id: pnpm-cache # use this to check for `cache-hit` (`steps.pnpm-cache.outputs.cache-hit != 'true'`)
        if: ${{ inputs.package-manager == 'pnpm' }}
        with:
          path: ${{ steps.pnpm-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-${{ inputs.node-version }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-${{ inputs.node-version }}-pnpm

      - name: Install node dependencies using pnpm
        if: ${{ inputs.package-manager == 'pnpm' }}
        run: |
          pnpm install

      - name: Run ESLint
        run: |
          npm run lint:fix

      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: |
            Fix styling 💄

            [ci skip]

